services:
  - type: web
    name: antidote-mission
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: GROQ_API_KEY
        sync: false   # vous la saisissez manuellement dans Render Dashboard
